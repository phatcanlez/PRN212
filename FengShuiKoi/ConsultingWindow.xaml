<Window x:Class="FengShuiKoi.ConsultingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FengShuiKoi"
        mc:Ignorable="d"
       Title="ConsultingWindow" Height="450" Width="800" WindowState="Maximized" WindowStartupLocation="CenterScreen" Background="Honeydew">
    <Grid Margin="30,36,30,112">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!-- Tiêu đề trang -->
        <TextBlock Text="Consulting and FAQS" FontWeight="Bold"
                   HorizontalAlignment="Left" Margin="584,0,0,262" Foreground="#333" FontFamily="Cascadia Code SemiBold" FontSize="20" Width="388"/>

        <!-- ListView để hiển thị danh sách các FAQ -->
        <ListView x:Name="FaqListView" Background="#ffffff" BorderThickness="0" Margin="0,32,0,-32">
            <!-- Câu hỏi và câu trả lời được đặt trong Expander -->
            ListView x:Name="FaqListView" Background="#ffffff" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Expander Header="{Binding Question}" FontSize="16" Padding="5" 
                              Background="#eeeeee" Foreground="#444" 
                              Margin="0,5,0,5">
                        <TextBlock Text="{Binding Answer}" TextWrapping="Wrap" 
                                   Padding="10" FontSize="14" Foreground="#666"/>
                    </Expander>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Window>
